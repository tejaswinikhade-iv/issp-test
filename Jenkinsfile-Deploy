pipeline{
    agent{
        label{
            label'jenkins-slave1'
        }
    }
     stages{
        stage('Connect-VM'){
            steps{
                 sshagent(['issp-ssh-key']) {
                sh '''
                  ssh -o StrictHostKeyChecking=no issp@192.168.1.31 "mkdir -p file1.txt"
                '''
                 }
            }
        }
     }
}